@model HaagsVertaler.Models.TranslationViewModel

<div class="form-header" style="margin-top: 100px;">
	<p>De vertaler staat even uit, zodat je niet kunt cheaten tijdens het Haags dictee :)</p>
</div>

@*@using (Html.BeginForm())
{
	<div class="form-header">
		<h2>Nederlands</h2>
		<button type="button" class="js-wis-tekst">Wis tekst</button>
	</div>

	@Html.TextAreaFor(f => f.Source, new { placeholder = "Het Nederlands", autofocus = true, @class = "js-nederlands", aria_label = "Nederlandse tekst" })
	<button type="submit" class="button">Naar Haags vertalen</button>


	if (!string.IsNullOrEmpty(Model.Result))
	{
		<h2>De Haagse vertaling</h2>
		<textarea aria-label="Haagse tekst">@Model.Result</textarea>
	}
}*@

